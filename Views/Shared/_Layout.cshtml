@using Microsoft.AspNetCore.Identity
@inject SignInManager<ApplicationUser> SignInManager
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - VacationManager</title>
    <!-- Google Fonts: Inter -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:opsz,wght@14..32,300;14..32,400;14..32,500;14..32,600;14..32,700&display=swap" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/VacationManager.styles.css" asp-append-version="true" />
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg">
            <div class="container">
                <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">VacationManager</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="navbar-collapse collapse">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <!-- Home -->
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Home" asp-action="Index">Home</a>
                        </li>
                        <!-- Dashboard (само за логнати) -->
                        @if (SignInManager.IsSignedIn(User))
                        {
                                <li class="nav-item">
                                    <a class="nav-link" asp-controller="Home" asp-action="Dashboard">Dashboard</a>
                                </li>
                        }
                        <!-- Моите заявки -->
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="VacationRequests" asp-action="MyRequests">My Vacations</a>
                        </li>

                        <!-- ⭐ CEO меню (пълен достъп) -->
                        @if (User.IsInRole("CEO"))
                        {
                                <li class="nav-item dropdown">
                                    <a class="nav-link dropdown-toggle" href="#" id="adminDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                        Admin
                                    </a>
                                    <ul class="dropdown-menu" aria-labelledby="adminDropdown">
                                        <li><a class="dropdown-item" asp-controller="Teams" asp-action="Index">Teams</a></li>
                                        <li><a class="dropdown-item" asp-controller="Teams" asp-action="Create">Create Team</a></li>
                                        <li><a class="dropdown-item" asp-controller="Teams" asp-action="AssignUser">Assign User</a></li>
                                        <li><hr class="dropdown-divider"></li>
                                        <li><a class="dropdown-item" asp-controller="Roles" asp-action="Index">Roles</a></li>
                                        <li><a class="dropdown-item" asp-controller="Users" asp-action="Index">Users</a></li>
                                        <li><a class="dropdown-item" asp-controller="Projects" asp-action="Index">Projects</a></li>
                                    </ul>
                                </li>
                        }

                        <!-- ⭐ Team Lead меню (ограничен достъп – вижда само Projects и Teams) -->
                        @if (User.IsInRole("Team Lead") && !User.IsInRole("CEO"))
                        {
                                <li class="nav-item">
                                    <a class="nav-link" asp-controller="Teams" asp-action="Index">My Team</a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" asp-controller="Projects" asp-action="Index">Projects</a>
                                </li>
                        }

                        <!-- Всички заявки (за CEO и Team Lead) -->
                        @if (User.IsInRole("CEO") || User.IsInRole("Team Lead"))
                        {
                                <li class="nav-item">
                                    <a class="nav-link" asp-controller="VacationRequests" asp-action="AllRequests">All Requests</a>
                                </li>
                        }

                        <!-- В отпуск -->
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="VacationRequests" asp-action="OnLeave">On Leave</a>
                        </li>
                    </ul>
                    <partial name="_LoginPartial" />
                </div>
            </div>
        </nav>
    </header>

    <main class="container my-4">
        @RenderBody()
    </main>

    <footer class="footer border-top py-3 text-muted">
        <div class="container text-center">
            &copy; @DateTime.Now.Year - VacationManager - <a asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>
        </div>
    </footer>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>